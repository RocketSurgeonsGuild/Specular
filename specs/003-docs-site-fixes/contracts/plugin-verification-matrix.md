# Contract: Plugin Verification Matrix

Every installed Starlight plugin must end with a recorded status of **Working**, **Fixed**, or
**Removed** (Removed only as a last resort, with a reason). Fill `evidence` and `status` during
implementation by exercising each feature live in the integrated browser MCP (light + dark mode where
visual) and confirming a clean `astro build`.

| #   | Plugin                      | Feature                        | Activation needed                                                        | Evidence                                                                                                                                                                                                                                                                                                                   | Status                                                |
| --- | --------------------------- | ------------------------------ | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 1   | `starlight-sidebar-topics`  | Topic-based sidebar            | Landing pages (this feature)                                             | Browser `/reference/`: topic nav renders all five topics (Getting Started, Reference, Architecture, API Reference, Changelog) with icons; each resolves to its landing page (HTTP 200, US1).                                                                                                                               | **Working**                                           |
| 2   | `starlight-links-validator` | Build-time broken-link check   | none                                                                     | `npm run build`: "All internal links are valid." Also clean under `GITHUB_ACTIONS=1` (`/Indago` base).                                                                                                                                                                                                                     | **Working**                                           |
| 3   | `starlight-github-alerts`   | `> [!NOTE]`-style alerts       | Added `> [!NOTE]` to `reference/index.md`                                | Browser `/reference/`: renders a styled "Note" aside (`<aside>` with Note label + body). Confirmed in light **and** dark mode.                                                                                                                                                                                             | **Working**                                           |
| 4   | `starlight-heading-badges`  | Badges on headings             | Added `## Pages :badge[Stable]{variant=success}` to `reference/index.md` | Browser `/reference/`: the "Pages" H2 renders a green "Stable" `.sl-badge`, mirrored in the "On this page" TOC. Confirmed in light **and** dark mode.                                                                                                                                                                      | **Working**                                           |
| 5   | `starlight-image-zoom`      | Click-to-zoom images           | Add one image (or document none)                                         | Installed and wired; no content images exist yet (`find src public` → none), so no zoom wrapper is emitted (DOM check: 0 wrappers). Documented limitation — the plugin activates automatically when the first image is added.                                                                                              | **Working** (wired; not yet demonstrable — no images) |
| 6   | `starlight-tags`            | Tag pages / filtering          | Extended `docs` schema with `starlightTagsExtension`                     | **Bug fixed**: tag pages reported "0 pages" because the `docs` collection schema did not expose the `tags` frontmatter field. Extended `src/content.config.ts` with `docsSchema({ extend: starlightTagsExtension })`. Browser `/tags/api/` now lists 3 pages + related tags; `/tags/` index + per-tag pages build cleanly. | **Fixed**                                             |
| 7   | `starlight-changelogs`      | Changelog from GitHub releases | `GH_API_TOKEN` (document if absent)                                      | Browser `/changelog/`: renders v0.0.2 (Latest) + v0.0.1 with real PRs and contributors. `GH_API_TOKEN` was **absent** locally yet the public-repo releases resolved; build generates `/changelog/` + version pages.                                                                                                        | **Working**                                           |
| 8   | `starlight-llms-txt`        | Generates `llms.txt`           | none                                                                     | `npm run build` emits `dist/llms.txt`, `dist/llms-full.txt`, `dist/llms-small.txt`.                                                                                                                                                                                                                                        | **Working**                                           |
| 9   | `starlight-auto-drafts`     | Hide drafts in prod            | Temporary `draft: true` page                                             | Temp `guide/zz-draft-verify.md` (`draft: true`): served in dev (HTTP 200) but absent from `dist/` after `npm run build` (excluded from production). Temp page removed after verification.                                                                                                                                  | **Working**                                           |
| 10  | `starlight-scroll-to-top`   | Scroll-to-top button           | Confirm on a long page                                                   | Build log: "Starlight Scroll To Top plugin has been installed successfully! Button position: right". Browser: scroll-to-top `<button>` present in the DOM (light + dark).                                                                                                                                                  | **Working**                                           |
| 11  | `starlight-page-actions`    | Edit / page-action links       | Confirm action links                                                     | Browser `/reference/` "Open" menu: "Copy Markdown", "Open in ChatGPT", "Open in Claude", "View in Markdown" (`/reference.md`). `editLink` configured to the GitHub `edit/main/docs/...` URL in `astro.config.mjs`.                                                                                                         | **Working**                                           |
| 12  | `starlight-plugin-icons`    | Extra icon set                 | Confirm an icon renders                                                  | Browser `/reference/`: the API Reference topic uses the `seti:brackets` icon (renders as `<svg>`), provided by the Seti icon set this plugin registers.                                                                                                                                                                    | **Working**                                           |

**Completion rule**: no blank `status`; every `Removed` row has a reason in the Evidence column;
intended outcome is all rows Working/Fixed. (Spec FR-005, FR-006, FR-007; SC-003.) ✅ **All 12 rows
Working/Fixed; no removals required.**

> Count reconciliation (T002): `astro.config.mjs` registers **12** distinct `starlight-*` plugins
> (listed above). The "14 plugins" figure in spec 002 / SC-007 was a stale/over-count — the actual
> installed-and-active set is 12, and every one is covered here. No plugin present in the config is
> missing from this matrix.
